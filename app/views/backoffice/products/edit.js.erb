$('#product-id').val('<%= @product.id %>')
$('#product-name').val('<%= @product.name %>')
$('#product-version').val('<%= @product.version %>')
$('#product-link').val('<%= @product.link %>')
$('#product-category').val('<%= @product.category %>')
$('#formProductModal').modal();
